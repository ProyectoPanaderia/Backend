/**
 * @typedef {Object} IRepartoDTO
 * @property {number} id
 * @property {string} nombre
 * @property {'S'|'N'} tercerizado - Indica si el reparto es tercerizado ('S' = sí, 'N' = no)
 * @property {'A'|'I'} estado - Estado del reparto ('A' = activo, 'I' = inactivo)
 */
module.exports = {};
